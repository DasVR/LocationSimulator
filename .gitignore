# Xcode - keep .xcodeproj for CI, ignore user data only
*.xcworkspace/xcuserdata/
*.xcuserdata/
DerivedData/
build/
*.ipa
*.dSYM

# Keep the pre-generated .xcodeproj for CI builds
# (individual subdirs like xcuserdata are ignored above)
# LocationSimulator.xcodeproj/  <-- intentionally NOT ignored

# Secrets
teamID.xcconfig
*.mobileprovision
*.p12
rp_pairing_file.plist

# macOS
.DS_Store

# Node / misc
node_modules/
*.log

# Pre-built binaries (downloaded during setup)
LocationSimulator/idevice/IDevice.xcframework/

# Claude local plans
.claude/
