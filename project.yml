name: LocationSimulator
createIntermediateGroups: true

targets:
  LocationSimulator:
    type: application
    platform: iOS
    deploymentTarget: "17.4"
    sources:
      - path: LocationSimulator/App
      - path: LocationSimulator/Views
      - path: LocationSimulator/ViewModels
      - path: LocationSimulator/Models
      - path: LocationSimulator/Services
      - path: LocationSimulator/idevice
    resources:
      - path: LocationSimulator/Resources/Config.plist
      - path: LocationSimulator/Info.plist
    entitlements:
      path: LocationSimulator/LocationSimulator.entitlements
    settings:
      base:
        SWIFT_VERSION: "5.9"
        PRODUCT_NAME: LocationSimulator
        INFOPLIST_FILE: LocationSimulator/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.yourcompany.locationsimulator
    dependencies:
      - sdk: SwiftUI.framework
      - sdk: MapKit.framework
      - sdk: CoreLocation.framework
      - sdk: AVFoundation.framework
      - sdk: NetworkExtension.framework
      - sdk: UniformTypeIdentifiers.framework
      - framework: LocationSimulator/idevice/IDevice.xcframework
      - target: LocationSimulatorVPN

  LocationSimulatorVPN:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.4"
    sources:
      - path: LocationSimulator/VPN/PacketTunnelProvider.swift
    resources:
      - path: LocationSimulator/VPN/Info.plist
    entitlements:
      path: LocationSimulator/VPN/VPNExtension.entitlements
    settings:
      base:
        SWIFT_VERSION: "5.9"
        PRODUCT_NAME: LocationSimulatorVPN
        INFOPLIST_FILE: LocationSimulator/VPN/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.yourcompany.locationsimulator.vpn
    dependencies:
      - sdk: NetworkExtension.framework

  LocationSimulatorTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.4"
    sources:
      - path: LocationSimulatorTests
    dependencies:
      - target: LocationSimulator
